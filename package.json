{
  "name": "agente-prospeccao-real",
  "version": "27.0.0",
  "description": "LeadHunter Pro: prospecção, auditoria digital, CRM 360 e operação comercial omnichannel para freelancers e agências",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "dev:landing": "npm --prefix frontend/landing run dev",
    "build": "node scripts/build.js",
    "build:react": "npm --prefix frontend/landing run build",
    "build:static": "node scripts/sync-static-landing.js",
    "verify:landing": "node scripts/verify-landing-build.js",
    "check:hygiene": "node scripts/check-repository-hygiene.js",
    "check:syntax": "node scripts/check-all.js",
    "check:docs": "node scripts/check-documentation.js",
    "check:architecture": "node scripts/check-architecture.js",
    "check:frontend": "node scripts/check-frontend.js",
    "check:styles": "node scripts/check-style-architecture.js",
    "test": "node --test tests/*.test.js",
    "quality": "npm run check:hygiene && npm run check:syntax && npm run check:docs && npm run check:architecture && npm run check:frontend && npm run check:styles && npm run verify:landing && npm test",
    "check": "npm run quality"
  },
  "dependencies": {
    "@fast-csv/format": "^5.0.5",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.3"
  },
  "engines": {
    "node": ">=20.19 <23",
    "npm": ">=10"
  }
}
